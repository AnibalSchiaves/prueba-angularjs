{
  "name": "basico",
  "version": "1.0.0",
  "description": "prueba básica con angularjs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy-libs": "cpx \"node_modules/{angular,angular-*}/**/*\" lib -C",
    "postinstall": "npm run copy-libs",
    "prestart": "npm install",
    "start": "http-server . -a localhost -p 7000 -c-1"
  },
  "author": "Anibal Schiaves",
  "license": "ISC",
  "dependencies": {
    "angular": "1.8",
    "angular-route": "1.8"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "http-server": "^14.1.1"
  }
}
